# Henry SDK Configuration
HENRY_API_KEY=your_sandbox_api_key_here # Get your Sandbox API key from https://sandbox.henrylabs.ai/
HENRY_ENV=sandbox                       # or production
# HENRY_SDK_BASE_URL=                   # Optional: custom base URL for SDK (overrides HENRY_ENV)

# Henry MCP Configuration (defaults to HENRY_ENV if not specified)
# HENRY_MCP_ENV=sandbox                 # Optional: separate environment for MCP (sandbox or production)

# AI Route Configuration (/ai)
ANTHROPIC_API_KEY=your_anthropic_api_key_here # Get your Anthropic API key from https://console.anthropic.com/
CLAUDE_MODEL=claude-sonnet-4-5
